@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Kurtarma Kodu Doğrulama";
}

<style>
    body {
        font-family: 'Arial', sans-serif;
        background-color: #f7f7f7;
    }

    .auth-container {
        max-width: 600px;
        margin: 10% auto;
        padding: 20px 40px;
        background-color: white;
        box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
        border-radius: 10px;
    }

    h1 {
        text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        color: black;
    }

    p {
        font-size: 1.1rem;
        color: black;
    }

    a {
        color: #007BFF;
        text-decoration: none;
    }

        a:hover {
            text-decoration: underline;
        }
</style>

<div class="auth-container">
    <h1>@ViewData["Title"]</h1>
    <hr />
    <p>
        Kurtarma koduyla giriş yapmayı talep ettiniz. Bir doğrulayıcı uygulama kodu girmediğiniz veya 2FA'yı devre dışı bırakmadığınız sürece
        bu giriş hatırlanmayacaktır.
    </p>
    <div class="row">
        <div class="col-md-4">
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-floating">
                    <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                    <label asp-for="Input.RecoveryCode" class="form-label"></label>
                    <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
                </div>
                <button type="submit" class="w-100 btn btn-lg btn-primary">Giriş Yap</button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
